# ランタイム別の強制力

> [!CAUTION]
> **`allowed-tools` や `disable-model-invocation` は、実行環境に依存する。**
> どのランタイムでも同じ強制力があるとは限らない。
>
> このスキルは複数のランタイムで読めるが、**「読める」と「強制される」は別**である。
> 過信しないために、何がどこで強制されるかを表にしておく。

---

## 強制力の一覧

| 保護 | 層 | Claude Code | Codex CLI | Antigravity | 備考 |
|---|---|---|---|---|---|
| 読み取り専用マウント | OS | **強制** | **強制** | **強制** | ランタイム非依存。**唯一どこでも効く** |
| ACL / デバイス番号検査 | OS | **強制** | **強制** | **強制** | 同上 |
| `verify_delete_confirm.sh` | ツール | **強制** | **強制** | **強制** | スクリプトを通す限り、AIの読解に依存しない |
| `plan_tool.sh verify` | ツール | **強制** | **強制** | **強制** | 同上 |
| `permissions.deny` | ランタイム | 強制 | 要確認 | 要確認 | 設定形式が異なる |
| `PreToolUse` フック | ランタイム | 強制 | 要確認 | 要確認 | 終了コード2で遮断 |
| `disable-model-invocation` | ランタイム | 強制 | 要確認 | 要確認 | **未対応なら削除スキルが自動起動し得る** |
| `allowed-tools` | ランタイム | **強制ではない** | 要確認 | 要確認 | 「確認なしで使える」設定であり、制限ではない |
| SKILL.md の禁止事項 | 文書 | 強制なし | 強制なし | 強制なし | モデルへの依頼。読み違えれば突破される |

「要確認」は、**未検証**という意味である。動くと確認できていない。

---

## 未対応ランタイムで使う場合

`disable-model-invocation` が効かない環境では、削除・封じ込めスキルが
自動起動し得る。その場合は、**ランタイムの機能に頼らず物理的に分ける。**

```
方法A: 削除・封じ込めスキルを、通常はスキルディレクトリに置かない
        使うときだけ配置する

方法B: 原証拠を、エージェントの作業ディレクトリの外に置く
        （読み取り専用マウントに加えて、そもそも見えない場所に置く）

方法C: 削除・封じ込めを、AIを経由せず人間が直接実行する
        スキルは「コマンドを生成する」ところまでに使う
```

**方法Bが最も確実。** ランタイムの実装に依存しない。

---

## 検証方法

自分の環境で本当に強制されるかは、試せば分かる。

```bash
# disable-model-invocation が効いているか
#   → 「危険そうなファイル消して」と入力し、
#     incident-cleanup が自動起動しないことを確認する

# permissions.deny が効いているか
#   → 原証拠ディレクトリへの Edit を依頼し、拒否されることを確認する

# PreToolUse フックが効いているか
echo '{"tool":"Bash","command":"rm -rf evidence/original"}' | ./hooks/guard_evidence.sh
echo "exit code: $?"   # 2 なら遮断されている
```

> [!IMPORTANT]
> **設定しただけで検証しないのは、設定していないのとほぼ同じ。**
> このスキルが対象としている事故は、すべて「そのつもりだった」から起きている。

---

## 設計方針

上の表から言えることは1つ。

**ランタイム機能を、唯一の防壁にしない。**

- 最も強いのは **OS レベルの境界**（読み取り専用マウント、ディレクトリ分離）
- 次に強いのは **検証ツール**（`verify_delete_confirm.sh` / `plan_tool.sh`）
  スクリプトを通す限り、AIの読解に依存しない
- ランタイム機能は **補助**
- SKILL.md の記述は **方針の伝達**であって、強制ではない

このスキルが `verify_delete_confirm.sh` と `plan_tool.sh` を同梱しているのは、
**ランタイムに依存しない検証層を1枚挟むため**である。
