# 開発中に踏んだ失敗の記録

このスキルは、AIエージェントが証拠ファイルを削除した事故から作られた。
そして開発中、**同じ形の失敗を何度も繰り返した。**

設計判断の背景として、そのまま残しておく。
「なぜこの設計になっているか」は、たいてい「何かで転んだから」である。

---

## 1. 「詰め合わせを作るな」と書きながら、詰め合わせを作っていた

**v3.0** — 収集スクリプトが `/etc/shadow`・全ユーザーの秘密鍵・AI CLI の認証トークンを、
暗号化なしの `tar.gz` にまとめて `/var/tmp` に置いていた。

侵害されたホスト上で、**攻撃者が欲しがるものを一つの便利なアーカイブにして提供する**
設計になっていた。`chmod 600` は root に対して意味がない。

**v3.1** — `tar` を `age` へ直接パイプし、「平文アーカイブを作らない」形にした。

**v3.2** — 査読で指摘。**パイプする前段で、これらを作業ディレクトリへ `cp` していた。**
平文 tar は作らないが、**平文の認証情報詰め合わせディレクトリは作っていた。**
しかも `rm -rf` では SSD / CoW / スナップショット上の残存を消せない。

→ **モードごと廃止。** 秘密情報は本文を一切収集せず、
存在・パス・権限・サイズ・mtime・SHA-256 のみを記録する形にした。

**教訓**: 「暗号化すれば安全」ではない。暗号化する前に平文が要る。
境界は「暗号化するかどうか」ではなく「本文を扱うかどうか」に引く。

---

## 2. 証拠を取るつもりで、証拠を上書きしていた

AI エージェントのセッションログを、すべて同じディレクトリへコピーしていた。

```
~/.claude/settings.json  →  ai_sessions/<user>/settings.json
~/.codex/settings.json   →  ai_sessions/<user>/settings.json  ← 上書き
```

`projects/` も同様にマージされていた。

証拠が消えるだけでなく、**どのエージェント由来かという来歴も失われる。**
そしてこのスキルは、まさに「どのエージェントが実行したか」を特定するために
セッションログを収集している。

→ エージェント別ディレクトリに分離。コピー先が存在したら `gap failed` に記録して中止。

---

## 3. 隔離のつもりで、穴を開けていた

単一の `IR_CONTAINMENT` チェーンを、`INPUT` と `OUTPUT` の両方から呼んでいた。

そのチェーンには Tailscale 維持のための送信許可が入っていた。

```
-p tcp --dport 443 -j RETURN
-p udp --dport 53  -j RETURN
-p udp --dport 123 -j RETURN
```

`INPUT` からも同じチェーンを通るため、これは
**外部からサーバーの 443/53/123 へ接続可能**というルールにもなっていた。

さらに `FORWARD` を処理していなかったため、
**Docker やブリッジ接続のコンテナ通信が隔離を丸ごと迂回**していた。

→ `IR_CONTAINMENT_IN` / `_OUT` / `_FWD` に分離。

**教訓**: 送信の例外は、方向を指定しないと受信の開放になる。

---

## 4. 自分を許可したつもりで、自分を遮断する手順を書いていた

```bash
TRUSTED_V4="203.0.113.10/32"  # あなたの接続元（curl -4 ifconfig.me）
```

**侵害サーバー上で `curl ifconfig.me` を実行すると、返ってくるのは
サーバー自身の外向き IP。** SSH クライアントの接続元ではない。

さらに `--dport 22` を固定していた。SSH ポートを変更している環境では、
現在の接続は ESTABLISHED で一時的に残っても、切断したら再接続できない。

→ `SSH_CONNECTION` から取得する形に変更。ただし**それも自動で信頼しない**
（その接続自体が攻撃者のものかもしれない）。ポートは `sshd -T` / `ss -tlnp` で確認。

---

## 5. `DELETE-CONFIRM` と書いてあったが、実装されていなかった

README には「絶対パス＋確認トークン」「DELETE-CONFIRM 必須」と書いてあった。
`incident-cleanup/SKILL.md` に、その規則が**存在しなかった。**

ドキュメントと実装が矛盾したまま、査読を通過していた。

→ 検証規則を実装。さらに指摘を受けて、
**通常ファイル限定**（ディレクトリを承認できると `rm -rf` で保護対象を巻き込める）、
**シンボリックリンク拒否**、**デバイス番号による bind mount 検査**を追加。

---

## 6. 承認識別子が、承認内容を表していなかった

封じ込めプランに SHA-256 を付ける設計にしたが、
ハッシュ対象の JSON に `status` を含めていた。

`proposed → approved` と更新した瞬間に JSON が変わり、**Plan ID も変わる。**
承認した ID と実行時の ID が、構造上必ず不一致になる。

さらに、プランには `"preserve_egress_ports": [41641, 443, 53, 123]` としか
書いていなかったのに、実行コマンドでは `3478/udp` も許可していた。
**承認したプランに無い通信許可が実行される**状態だった。

→ 不変部 `plan` と可変部 `lifecycle` を分離し、`plan` のみをハッシュ。
ルールは family / hook / protocol / conntrack 状態の粒度で構造化。
実行スクリプトとロールバックスクリプト自体の SHA-256 も承認対象に含めた。

---

## 7. 片方のレイヤーだけ直して、隣を確認しなかった（複数回）

- `incident-containment/SKILL.md` の ufw を専用チェーン方式に書き換えたが、
  **同じ内容を書いていた `containment_matrix.md` を直し忘れた**
- 記事側で確度表現を「支持されない」に統一したが、**README は「〜した」と断定したままだった**
- `sensitive` モードを廃止したが、**README / forensic_report / TODO に残骸が残っていた**

→ 同じ内容を2箇所に書かない。`containment_matrix.md` からコマンド例を削除し、
`SKILL.md` への参照に一本化した。

**教訓**: これは、このスキルが対象とする事故そのものと同じ構造である。
ひとつのレイヤーの証拠を見て、隣のレイヤーを確認せずに結論を出す。

---

## 8. そして、書いた本人が締め出された

**これが一番大きい。**

`iptables` のコードは、AI 3体で査読を3周し、構文チェックも通していた。
ただ、**一度も実行していなかった。**

検証のためにサンドボックスで流したところ、適用した直後から
**次のコマンドが実行できなくなった。** 復旧コマンドも届かない。

### 誤診した

最初の診断は「`conntrack` が使えない環境だったため、
`ESTABLISHED,RELATED` の保護が機能しなかった」だった。

**これは間違いだった。**

環境が復旧してから計測したところ、conntrack は正常に動作していた。

```
IR_CT_PROBE の実測
  ESTABLISHED,RELATED : 15 packets
  NEW                 :  2 packets
```

適用後に `curl https://example.com` も通った。**送信側は問題なかった。**

### 実際の原因

**受信側の許可漏れ。**

`IR_CONTAINMENT_IN` は SSH 以外の新規受信をすべて DROP する。
作業に使っていたツールは、**このホストに対して接続してくる**側だった。
新しいコマンド実行のたびに新規の受信接続が張られ、それが落ちていた。

`ESTABLISHED,RELATED` は「いま繋がっている接続」しか守らない。
**新しく張られてくる接続は、すべて DROP に落ちる。**

これは特殊な状況ではない。実サーバーでも同じことが起きる。

- 構成管理エージェント（オーケストレータから接続してくる型）
- 監視のポーリング / ヘルスチェック
- ロードバランサからのバックエンド接続
- クラウド / コンテナの管理プレーンからの接続

### 復旧したのはデッドマンスイッチ

2回目の再現テストでは、適用前に自動ロールバックを仕掛けておいた。

```bash
setsid nohup bash -c 'sleep 45; bash /tmp/rb.sh' >/dev/null 2>&1 &
```

締め出されたが、45秒後に自動で戻った。ログに記録が残っている。

```
[2026-08-01T06:47:55+00:00] rollback done
```

**これが唯一の復旧手段だった。**

### 検証できたこと（2回の実測）

| 項目 | 結果 |
|---|---|
| IN/OUT チェーン分離 | ✅ 443 は OUT にのみ存在、IN には無い |
| 適用後の送信 | ✅ `curl https://` は通る |
| 適用後の新規受信 | ❌ 落ちる（**これが締め出しの原因**） |
| conntrack の動作 | ✅ ESTABLISHED 15pkt / NEW 2pkt でマッチ |
| デッドマンスイッチ | ✅ **2回とも自動発火して完全復旧。残骸チェーン0** |

```
[2026-08-01T06:47:55+00:00] rollback done   ← 1回目
[2026-08-01T06:54:01+00:00] rb2 done        ← 2回目
```

デッドマンの実装で重要だったのは `setsid nohup` を付けること。
親シェルが締め出しで死んでも、ロールバックだけは生き残る必要がある。

```bash
setsid nohup bash -c 'sleep 40; bash /tmp/rb.sh' >/dev/null 2>&1 &
```

### 教訓

- 構文チェックとレビューを通っても、**実行してみるまで分からない層がある**
- 最初の診断は外れることがある。**「原因が分かった」と書く前に測る**
- 送信が通っていても受信が死んでいることがある。**片側だけ見て結論を出さない**
- **本番が初回実行になってはいけない。** 使い捨て環境で一度転んでおく
- **デッドマンスイッチを、適用の前に仕掛ける**

→ `references/rehearsal.md` と `scripts/rehearse_containment.sh` を追加した。

---

## 9. 「10年前は正しかった情報」を、確認せず持ち回っていた

スキルとドキュメントに、こう書いていた。

> sshd の `LogLevel` が `VERBOSE` でないと、auth.log に鍵フィンガープリントが
> 残らず、実行者の対応付けができない

**誤りだった。**

### 実測

構築編の動画用に GCP でサーバーを建て、実際に確認した。
`LogLevel` を INFO → VERBOSE → INFO と切り替えて、毎回 SSH で入り直した。

```
11:16:58  LogLevel INFO      → Accepted publickey ... ssh2: ECDSA SHA256:WAeF6k...
11:22:18  LogLevel VERBOSE   → Accepted publickey ... ssh2: ECDSA SHA256:NBqzQ+...
11:24:03  LogLevel INFO(戻し) → Accepted publickey ... ssh2: ECDSA SHA256:dHfsBu...
```

**3回とも同じ形式でフィンガープリントが出力された。**
LogLevel は、成功した認証の FP 出力に関係していなかった。

### なぜ間違えたか

調べ直したところ、**バージョンによって挙動が変わっていた。**

| OpenSSH | 挙動 |
|---|---|
| 6.2 以前 | `Accepted publickey` に FP が入らない。**VERBOSE が必要**（別行 `Found matching RSA key:` として出力） |
| **6.3 以降** | **`Accepted publickey` の行に FP が入る（INFO で足りる）** |
| 6.8 以降 | FP の形式が MD5 → SHA256 に |

つまり **2013年の OpenSSH 6.2 までは「VERBOSE が必要」で正しかった。**

当時書かれた記事は嘘ではない。**仕様が変わっただけ。**
そして古い記事は今も検索上位に残っている。

### これまでの誤りとの違い

| # | 誤り | 種類 |
|---|---|---|
| — | `-o` を "overlapping" の略と書いた | **最初から誤り**。4ヶ月持ち回った |
| 8 | 締め出しの原因を conntrack と診断した | **その場の誤診**。測って訂正した |
| **9** | **VERBOSE でないと FP が残らない** | **かつては正しかった情報の陳腐化** |

9番が最もたちが悪い。
**出典を確認しても「間違っている」と分からない。** 当時は正しかったのだから。

### 教訓

- **「調べた」と「試した」は別。** 今回も、建てて叩くまで気づかなかった
- 情報には**賞味期限**がある。特にバージョン依存の挙動は、
  出典が正しくても現在の挙動とは限らない
- **手を動かす回（構築編の準備）が、書いたものの検証になった。**
  作る過程が、作ったものを直している

### 結果として、記事の主張は変わらなかった

4月の事故でフィンガープリントを追えなかった本当の理由は、
`LogLevel` ではなく **`auth.log` そのものを保全しなかったこと** だった。

記事の主題（救出はしたが保全はしなかった）とは、**むしろ一致が強くなった。**
補足の技術的説明が1つずれていただけで、結論は変わっていない。

---

## まとめ

9件のうち、**7件が「自分が対象としている失敗と同じ形」だった。**
そして9件目は、**対策を作り終えたあと、動画の準備をしている最中に見つかった。**

- 詰め合わせを作るなと書いて、詰め合わせを作る
- 証拠を守るスキルで、証拠を上書きする
- 隔離するコードで、穴を開ける
- 締め出しを警告するドキュメントで、締め出される
- 片方のレイヤーだけ直して、隣を確認しない

これは偶然ではないと思う。**何かを警告できるのは、その形を理解しているからで、
理解しているからといって、自分がやらないわけではない。**

だからこのスキルは、指示ではなく構造で守る設計になっている。
「消すな」と書くのではなく、消せない場所に置く。
「気をつけろ」と書くのではなく、45秒後に自動で戻す。
